#include <stdio.h>

int main () {

    int x, y, z;

    printf("dia:");
    scanf("%d", &x);

    printf("mes:");
    scanf("%d", &y);

    printf("ano:");
    scanf("%d", &z);

    printf("data: %d \\ %d \\ %d", x, y, z);
}